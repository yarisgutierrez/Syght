Create binaries for Linux/Mac/Windows:
Install the pip3 package pyinstaller.
Then from the project directory run:
pyinstaller --onefile big_explorer.py
This will create a dist/big_explorer binary that can be distributed for the arch with all the libs. One file. 

